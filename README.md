# GitHub Required Check Demo

This repository is used to test GitHub branch protection rules with GitHub Actions required status checks.

## Purpose

The demo verifies that a pull request cannot be merged until the configured GitHub Actions check passes.

## Workflow

The workflow file is located at:

```text
.github/workflows/required-check-demo.yml
